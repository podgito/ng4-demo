import { DataPointFilterPipe } from './data-point-filter.pipe';

describe('DataPointFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DataPointFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
